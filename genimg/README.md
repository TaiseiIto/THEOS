# GenImg

This tool generates FAT32 raw image file of the THEOS bootable drive containing a specified directory as the root of the drive.

## Usage

```
$ ./genimg boot_sector.bin fsinfo_sector.bin input_root_directory output_disk_image.bin
```

