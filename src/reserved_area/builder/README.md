# FAT32 reserved area builder

This program inputs raw binary images of boot sector and FSInfo sector and outputs a raw binary image of FAT32 reserved area.

## Usage

```
$ ./builder ../boot_sector.bin ../fsinfo_sector.bin reserved_area.bin
```

