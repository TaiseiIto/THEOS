# enpushable.sh

This program gives the push access right to the executer.
Only developers who has the private key to push commits to the repository can execute it.
Put the private key `/root/.ssh/github` before the execution.

